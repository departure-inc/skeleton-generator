# Docker

## setup
```bash
$ make setup
```

## usage

```bash
$ make up
```

各種コマンドは`Makefile`を参照。

既存のファイルと置き換える場合、以下のファイルをリネームして利用します。
```
- Dockerfile.dev
- compose.dev.yml
```

## etc
VSCode Dev Containerを利用する場合は`.devcontainer`を参照してください。
